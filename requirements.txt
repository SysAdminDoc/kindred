fastapi==0.115.6
uvicorn[standard]==0.34.0
sentence-transformers==3.3.1
numpy>=1.26.0
python-multipart==0.0.20
pyjwt==2.9.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1
slowapi==0.1.9
# Shared production rate-limit and refresh-session backend (SQLite remains the default).
redis>=5.2,<7
# Distributed embedding and photo-moderation workers over the shared Redis broker.
dramatiq[redis]>=2.2,<3
# S3-compatible photo/video storage; local disk remains the default when no bucket is configured.
boto3>=1.35,<2
Pillow>=10.0.0
# Used only by the explicit SQLite-to-PostgreSQL migration command; SQLite remains the default.
psycopg[binary]>=3.2,<4
